<UsersScreen>:
    users_list: users_list
    sv: sv

    ScrollView:
        id: sv
        on_touch_down: root.update_screen()
        on_touch_move: root.update_screen()

        MDList:
            id: users_list
            pos_hint: {"top": 1}
